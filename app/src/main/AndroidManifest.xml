<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.windows.foodish">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="Hunger's Hope"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".RSignUp2" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".ListActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".DonorProfileActivity" />
        <activity android:name=".DonationActivity" />
        <activity android:name=".FreeDonationActivity" />
        <activity android:name=".FindPageActivity" />
        <activity android:name=".FindHunger" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".Gallery" />
        <activity android:name=".FullImageActivity" />
        <activity android:name=".Gallery1" />
        <activity android:name=".FullImageActivity1" />
        <activity android:name=".DonorSignUp" />
        <activity android:name=".RSignIn" />
        <activity android:name=".RSignUp" />
        <activity android:name=".DonorMore" />
        <activity android:name=".rEditProfile" />
        <activity android:name=".EditInfo" />
        <activity android:name=".MsgActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Main3ActivityFragment" />
        <activity android:name=".ListActivity2" />
        <activity android:name=".Confirm_Activity" />
        <activity android:name=".ListActivity3"></activity>
    </application>

</manifest>